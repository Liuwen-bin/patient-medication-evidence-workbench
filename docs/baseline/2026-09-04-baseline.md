# 2026-09-04 可执行基线

## 基线身份

- 运行日期：2026-09-04（Asia/Shanghai）
- 代码基线提交：`8c29dec chore: establish medication review agent baseline`
- 执行起点提交：`fec34ca96d5eb19c3f986623153dda0ae6a0f1a1`
- 实施分支：`codex/implement-medication-workbench`
- Python：3.11.7
- FastAPI：0.141.1
- Pydantic：2.13.4
- LangGraph：1.2.7
- langchain-openai：1.3.3
- pytest：9.1.1
- Playwright：1.62.0

`8c29dec` 是现有应用行为的代码基线；`fec34ca` 只在其上增加已确认的规格和一周实施计划，因此作为本轮执行起点单独记录。

## Core/API/LangGraph 基线

在独立 PowerShell 进程中运行：

```powershell
$env:PYTEST_DISABLE_PLUGIN_AUTOLOAD = '1'
python -m pytest tests `
  --ignore=tests/test_web_render.py `
  --ignore=tests/test_web_decisions.py `
  --ignore=tests/test_web_visual.py `
  -q -p pytest_asyncio.plugin
```

结果：118 项通过。测试输出包含一条来自 Starlette TestClient 的既有 `httpx` 弃用警告，不影响基线通过状态。

## Browser/Visual 基线

在另一个新的 PowerShell 进程中运行，避免 Playwright 与 pytest asyncio 插件共享事件循环状态：

```powershell
Remove-Item Env:PYTEST_DISABLE_PLUGIN_AUTOLOAD -ErrorAction SilentlyContinue
python -m pytest `
  tests/test_web_render.py `
  tests/test_web_decisions.py `
  tests/test_web_visual.py -q
```

结果：20 项通过。测试输出包含同一条 Starlette TestClient 弃用警告。

## 结果边界

这 138 项测试使用合成病例与 Fixture gateway，证明的是当前代码的确定性行为、接口约束和浏览器呈现可回归。它们不连接真实 LLM、Health Record MCP、Drug Evidence MCP、Neo4j、Milvus 或 DailyMed，不能被解释为在线正确率、临床有效性、真实性能或真实成本结果。

后续在线评估必须使用独立命令和独立输出文件，并同时记录模型、MCP 服务版本、数据快照、延迟、token、成本与失败原因；不得把在线结果和本基线汇总成同一组指标。
